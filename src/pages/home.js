import { Typography } from "@mui/material";

export default function Home() {
    return(
      <Typography sx={{textAlign: 'center'}}>
        Site mostrando alguns projetos feito por mim FlowCreper (Caio David)
      </Typography>
    )
}